/**************************************************************************/
      /*!
        \param val
			The value to be checked.
        \return
			Returns if given value is even or not.
        \brief 
			Checks that a given value is even.
      */
/**************************************************************************/
bool is_even(int val)
{
	return val % 2 == 0;
}
