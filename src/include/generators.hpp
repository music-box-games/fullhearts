#ifndef _WE_GENERATORS_HPP_
#define _WE_GENERATORS_HPP_

#include <string>

namespace waifuengine
{
  namespace utils
  {
    std::string generate_string(int length = 10);
    int generate_int();
  }
}

#endif